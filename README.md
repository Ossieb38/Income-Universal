# IncomeOS Universal - Free Edition

🌍 Earn globally. Cash out anywhere. Zero cost to start.

## 🚀 Quick Start (Replit)

1. **Import this repo** into Replit
2. **Add secrets** in Replit (PRIVATE_KEY, ALCHEMY_KEY)
3. **Run**: `npm install && npm run deploy:testnet`
4. **Start PWA**: `npm run dev`

## 📦 Installation (Local)

```bash
git clone https://github.com/yourusername/incomeos-universal.git
cd incomeos-universal
npm install
